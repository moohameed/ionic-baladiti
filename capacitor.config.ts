import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'baladiti2018',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
